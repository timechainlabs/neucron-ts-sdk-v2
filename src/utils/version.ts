/**
 * SDK identity constants used in the User-Agent header. Keep SDK_VERSION in
 * sync with package.json (checked by tests/unit/version.test.ts).
 */
export const SDK_NAME = 'neucron-ts-sdk';
export const SDK_VERSION = '1.1.2';
